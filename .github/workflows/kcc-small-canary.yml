name: kcc-small-canary

on:
  workflow_dispatch:

jobs:
  canary:
    runs-on: [self-hosted, Linux, X64, dragonsbreath, docker, kcc, kcc-small]
    steps:
      - run: |
          echo "runner=${RUNNER_NAME:-unknown}"
          uname -a
      - run: docker version
